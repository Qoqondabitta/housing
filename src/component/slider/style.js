import {styled} from "styled-components"

const SliderContainer = styled.div`

`;

const SliderIconWrap = styled.div`
  width: 50px;
  height: 50px;
  border-radius: 50px;
  fill: var(--color-5, #fff);
  opacity: 0.2;
  padding: 16px;
`;

export { SliderContainer, SliderIconWrap }